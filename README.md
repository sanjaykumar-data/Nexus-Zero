# Nexus Zero

On-device, privacy-locked document intelligence. Parses PDFs, chunks and
embeds them, retrieves via hybrid BM25 + cosine fusion, and answers
questions using a local LLM over WebGPU — all inside the browser tab, with
zero document content ever leaving the device.

## Quick start
